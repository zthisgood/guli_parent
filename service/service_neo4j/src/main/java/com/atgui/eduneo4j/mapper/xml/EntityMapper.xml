<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atgui.eduneo4j.mapper.EntityMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.atgui.eduneo4j.entity.Entity">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="kind" property="kind" />
        <result column="state" property="state" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, kind, state
    </sql>

    <select id="listEntity" resultMap="BaseResultMap">
        select id, name, kind, state from entity
    </select>
</mapper>
